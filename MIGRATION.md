# 📋 Инструкция по миграции на React

## Шаг 1: Подготовка файлов

### 1.1 Скопируйте статические файлы

Из вашего старого проекта скопируйте в новый:

```bash
# Из старого проекта в новый
cp -r img/ ../kairbekov-portfolio/public/
cp -r font/ ../kairbekov-portfolio/public/
```

Или вручную скопируйте:
- Папку `img/` → в `public/img/`
- Папку `font/` → в `public/font/`

### 1.2 Структура файлов после копирования

```
kairbekov-portfolio/
├── public/
│   ├── img/
│   │   ├── background/
│   │   ├── designs/
│   │   ├── ico/
│   │   ├── license/
│   │   ├── mockup/
│   │   ├── sites/
│   │   ├── slider/
│   │   └── me.webp
│   └── font/
│       └── akony/
│           ├── AKONY.woff2
│           └── AKONY.woff
```

## Шаг 2: Установка и запуск

### 2.1 Установите зависимости

```bash
cd kairbekov-portfolio
npm install
```

### 2.2 Запустите проект локально

```bash
npm run dev
```

Откройте браузер на `http://localhost:5173`

### 2.3 Проверьте, что всё работает

- ✅ Изображения загружаются
- ✅ Шрифты применяются
- ✅ Меню работает
- ✅ Модальные окна открываются
- ✅ Плавная прокрутка работает
- ✅ Адаптивность на мобильных

## Шаг 3: Сборка для продакшена

```bash
npm run build
```

Это создаст папку `dist/` с готовыми файлами.

## Шаг 4: Деплой на Netlify

### Вариант А: Через интерфейс Netlify (Рекомендуется)

1. **Создайте репозиторий на GitHub:**

```bash
git init
git add .
git commit -m "Initial commit - React version"
git branch -M main
git remote add origin git@github.com:zhkairbekov/kairbekov-official-react.git
git push -u origin main
```

2. **Подключите к Netlify:**
   - Зайдите на https://app.netlify.com
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите GitHub и ваш репозиторий
   - Настройки сборки:
     - **Build command:** `npm run build`
     - **Publish directory:** `dist`
   - Нажмите "Deploy site"

3. **Настройте домен (опционально):**
   - В настройках сайта на Netlify
   - Domain management → Add custom domain
   - Можете использовать бесплатный поддомен: `yourname.netlify.app`

### Вариант Б: Через Netlify CLI

```bash
# Установите Netlify CLI
npm install -g netlify-cli

# Войдите в аккаунт
netlify login

# Инициализируйте проект
netlify init

# Соберите и задеплойте
npm run build
netlify deploy --prod
```

### Вариант В: Drag & Drop (Быстрый тест)

```bash
# Соберите проект
npm run build

# Зайдите на https://app.netlify.com/drop
# Перетащите папку dist/ в окно браузера
```

## Шаг 5: Настройка после деплоя

### 5.1 Обновите URL в мета-тегах

Если у вас будет другой URL, обновите в `index.html`:

```html
<!-- Замените на ваш URL -->
<meta property="og:url" content="https://ваш-сайт.netlify.app/">
<link rel="canonical" href="https://ваш-сайт.netlify.app/">
```

### 5.2 Проверьте SEO

После деплоя проверьте:
- Google Search Console
- Yandex Webmaster
- meta теги через https://metatags.io/

## Шаг 6: Оптимизация (Опционально)

### 6.1 Оптимизация изображений

Для ещё лучшей производительности можете оптимизировать изображения:

```bash
# Установите imagemin
npm install -g imagemin-cli imagemin-webp

# Оптимизируйте все webp
imagemin public/img/**/*.webp --out-dir=public/img/optimized
```

### 6.2 Добавьте Google Analytics (если нужно)

В `index.html` перед закрывающим `</head>`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

## Шаг 7: Обновление контента

### Чтобы обновить контент в будущем:

1. **Редактируйте нужный компонент:**
   - Услуги → `src/components/Services.jsx`
   - Проекты → `src/components/Portfolio.jsx`
   - Дизайны → `src/components/Designs.jsx`
   - Обо мне → `src/components/About.jsx`

2. **Добавьте новые изображения:**
   - Поместите в `public/img/`
   - Используйте путь `/img/ваш-файл.webp`

3. **Задеплойте изменения:**
```bash
git add .
git commit -m "Update content"
git push
# Netlify автоматически пересоберёт сайт
```

## Частые проблемы и решения

### ❌ Не загружаются изображения

**Проблема:** Пути к файлам неверные

**Решение:** 
- Убедитесь, что файлы в `public/img/`
- Используйте пути `/img/...`, не `./img/...`

### ❌ Шрифты не применяются

**Проблема:** Неверные пути к шрифтам

**Решение:**
- Проверьте `src/index.css`
- Пути должны быть: `/font/akony/AKONY.woff2`

### ❌ Ошибки при сборке

**Решение:**
```bash
# Удалите node_modules и переустановите
rm -rf node_modules package-lock.json
npm install
npm run build
```

### ❌ Сайт не работает после деплоя на Netlify

**Проблема:** Netlify не перенаправляет маршруты

**Решение:** Убедитесь, что файл `netlify.toml` присутствует в корне проекта

## Преимущества React-версии

✅ **Производительность:**
- Быстрее загружается (code splitting)
- Оптимизированные ресурсы

✅ **Разработка:**
- Легче добавлять новые секции
- Компонентная структура
- Hot Module Replacement

✅ **SEO:**
- Все мета-теги сохранены
- Server-side rendering готов к добавлению

✅ **Поддержка:**
- Современный стек технологий
- Легко масштабировать
- Активное сообщество

## Дополнительные возможности

### Добавить анимации

```bash
npm install framer-motion
```

### Добавить форму обратной связи

Используйте Netlify Forms (бесплатно):
```html
<form name="contact" method="POST" data-netlify="true">
  <input type="text" name="name" />
  <input type="email" name="email" />
  <textarea name="message"></textarea>
  <button type="submit">Отправить</button>
</form>
```

### Добавить блог

Можно интегрировать:
- Contentful
- Strapi
- Ghost CMS

## Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи Netlify
3. Создайте issue в репозитории

## Чек-лист перед запуском

- [ ] Скопированы все изображения в `public/img/`
- [ ] Скопированы шрифты в `public/font/`
- [ ] `npm install` выполнен успешно
- [ ] `npm run dev` запускается без ошибок
- [ ] Все изображения отображаются
- [ ] Меню работает
- [ ] Модальные окна открываются
- [ ] Адаптивность проверена
- [ ] `npm run build` выполняется без ошибок
- [ ] Создан GitHub репозиторий
- [ ] Сайт задеплоен на Netlify
- [ ] URL обновлён в мета-тегах
- [ ] Проверена работа на мобильных

Готово! 🎉 Ваш сайт теперь на React и готов к деплою!
